import DashboardLayout from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Switch } from "@/components/ui/switch";
import { trpc } from "@/lib/trpc";
import { Bot, ServerCog } from "lucide-react";
import { useEffect, useState } from "react";
import { toast } from "sonner";

const arrayText = (value: string[]) => value.join(", "); const parseArray = (value: string) => value.split(",").map(item => item.trim()).filter(Boolean);
export default function Settings() { const servers = trpc.servers.list.useQuery(); const [serverId, setServerId] = useState(""); const config = trpc.servers.getConfiguration.useQuery({ serverId }, { enabled: Boolean(serverId) }); const [form, setForm] = useState<any>(null); const [discordUrl, setDiscordUrl] = useState(""); const utils = trpc.useUtils(); useEffect(() => { if (!serverId && servers.data?.[0]) setServerId(servers.data[0].id); }, [serverId, servers.data]); useEffect(() => { if (config.data) setForm(config.data.settings); }, [config.data]); const save = trpc.servers.updateConfiguration.useMutation({ onSuccess: () => { toast.success("Ayarlar kaydedildi."); utils.servers.getConfiguration.invalidate({ serverId }); }, onError: error => toast.error(error.message) }); if (!servers.data?.length) return <DashboardLayout><div className="p-9 text-sm text-stone-500">Ayarları yapılandırmak için önce bir BDS sunucusu ekleyin.</div></DashboardLayout>; if (!form) return <DashboardLayout><div className="p-9 text-sm text-stone-500">Ayarlar yükleniyor…</div></DashboardLayout>; const update = (path: string, value: unknown) => { const clone = structuredClone(form); const keys = path.split("."); let target = clone; keys.slice(0, -1).forEach(key => { target = target[key]; }); target[keys.at(-1)!] = value; setForm(clone); }; return <DashboardLayout><div className="mx-auto max-w-6xl px-5 py-7 lg:px-9"><p className="text-xs font-semibold uppercase tracking-[0.19em] text-emerald-300/75">Yapılandırma</p><div className="mt-2 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><h1 className="font-display text-3xl font-semibold tracking-tight">Kurallar ve ayarlar</h1><p className="mt-2 text-sm text-stone-500">Her BDS sunucusu kurallarını, eşiklerini ve bildirimlerini bağımsız yönetir.</p></div><select value={serverId} onChange={e => setServerId(e.target.value)} className="h-10 rounded-lg border border-white/10 bg-white/[0.04] px-3 text-sm">{servers.data.map(server => <option key={server.id} value={server.id}>{server.name}</option>)}</select></div><div className="mt-7 grid gap-6 xl:grid-cols-2"><section className="rounded-2xl border border-white/[0.07] bg-white/[0.025] p-5"><div className="flex items-center gap-3"><ServerCog className="h-5 w-5 text-emerald-300" /><div><h2 className="font-display text-lg font-semibold">Puanlama ve korumalar</h2><p className="text-xs text-stone-500">Puan zamanla azalır; tekil sinyal ban sebebi değildir.</p></div></div><div className="mt-5 grid grid-cols-2 gap-3"><Field label="Yarılanma (saat)" value={form.scoreHalfLifeHours} onChange={v => update("scoreHalfLifeHours", Number(v))} /><Field label="İzleme eşiği" value={form.thresholds.watch} onChange={v => update("thresholds.watch", Number(v))} /><Field label="Müdahale eşiği" value={form.thresholds.intervention} onChange={v => update("thresholds.intervention", Number(v))} /><Field label="İnceleme eşiği" value={form.thresholds.review} onChange={v => update("thresholds.review", Number(v))} /><Field label="Geçici ban eşiği" value={form.thresholds.tempBan} onChange={v => update("thresholds.tempBan", Number(v))} /><Field label="Kick tekrar sayısı" value={form.recurrence.minEventsForKick} onChange={v => update("recurrence.minEventsForKick", Number(v))} /></div><Toggle label="Farklı sinyal koruması" checked={form.safeguards.requireDistinctSignalsForIntervention} onChange={v => update("safeguards.requireDistinctSignalsForIntervention", v)} /><Toggle label="Otomatik uyarı oluştur" checked={form.safeguards.autoEnforceWarnings} onChange={v => update("safeguards.autoEnforceWarnings", v)} /><Toggle label="Otomatik geçici ban" checked={form.safeguards.allowAutomatedTempBans} onChange={v => update("safeguards.allowAutomatedTempBans", v)} note="Kapalı kalması önerilir; açık olsa bile olay geçmişi koşulu aranır." /></section><section className="rounded-2xl border border-white/[0.07] bg-white/[0.025] p-5"><div className="flex items-center gap-3"><Bot className="h-5 w-5 text-violet-300" /><div><h2 className="font-display text-lg font-semibold">Kurallar, AI ve Discord</h2><p className="text-xs text-stone-500">AI yalnızca ek risk sinyali sağlar, nihai karar vermez.</p></div></div><div className="mt-5 space-y-3"><TextField label="Küfür/hakaret sözleri" value={arrayText(form.rules.profanityWords)} onChange={v => update("rules.profanityWords", parseArray(v))} /><TextField label="Yasaklı kelimeler" value={arrayText(form.rules.bannedWords)} onChange={v => update("rules.bannedWords", parseArray(v))} /><TextField label="Yasaklı komutlar" value={arrayText(form.rules.prohibitedCommands)} onChange={v => update("rules.prohibitedCommands", parseArray(v))} /><div className="grid grid-cols-2 gap-3"><Field label="Flood penceresi (sn)" value={form.rules.spamWindowSeconds} onChange={v => update("rules.spamWindowSeconds", Number(v))} /><Field label="Flood limit" value={form.rules.spamMessageLimit} onChange={v => update("rules.spamMessageLimit", Number(v))} /></div><Toggle label="LLM destekleyici risk analizi" checked={form.ai.enabled} onChange={v => update("ai.enabled", v)} note="Mesaj içeriği yalnızca sunucu tarafında sınıflandırılır." /><Field label="AI minimum güven" value={form.ai.minimumConfidence} onChange={v => update("ai.minimumConfidence", Number(v))} step="0.01" /><TextField label={config.data?.server.discordConfigured ? "Discord webhook (değiştirmek için yeni URL)" : "Discord webhook URL"} value={discordUrl} onChange={setDiscordUrl} placeholder="https://discord.com/api/webhooks/..." /></div></section></div><div className="mt-6 flex justify-end"><Button disabled={save.isPending} onClick={() => save.mutate({ serverId, settings: form, ...(discordUrl.trim() ? { discordWebhookUrl: discordUrl.trim() } : {}) })} className="bg-emerald-400 text-[#102014] hover:bg-emerald-300">{save.isPending ? "Kaydediliyor…" : "Ayarları kaydet"}</Button></div></div></DashboardLayout>; }
function Field({ label, value, onChange, step }: { label: string; value: number; onChange: (value: string) => void; step?: string }) { return <label className="text-xs text-stone-500">{label}<Input type="number" step={step} value={value} onChange={e => onChange(e.target.value)} className="mt-1.5 border-white/10 bg-white/[0.04] text-stone-200" /></label>; }
function TextField({ label, value, onChange, placeholder }: { label: string; value: string; onChange: (value: string) => void; placeholder?: string }) { return <label className="block text-xs text-stone-500">{label}<Input value={value} onChange={e => onChange(e.target.value)} placeholder={placeholder} className="mt-1.5 border-white/10 bg-white/[0.04] text-stone-200" /></label>; }
function Toggle({ label, checked, onChange, note }: { label: string; checked: boolean; onChange: (checked: boolean) => void; note?: string }) { return <div className="mt-4 flex items-start justify-between gap-4 rounded-xl bg-white/[0.035] p-3"><div><p className="text-sm text-stone-300">{label}</p>{note && <p className="mt-1 text-xs leading-5 text-stone-600">{note}</p>}</div><Switch checked={checked} onCheckedChange={onChange} /></div>; }
